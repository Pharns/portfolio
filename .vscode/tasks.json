{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Backup (git archive)",
        "type": "shell",
        "command": "mkdir -p backups && git archive --format=zip --output \"backups/portfolio-$(date +%F_%H%M)-$(git rev-parse --short HEAD).zip\" HEAD && ls -lh backups | tail -n 1",
        "group": "build",
        "presentation": { "reveal": "always" }
      }
    ]
  }